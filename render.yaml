services:
  - type: web
    name: ttcecs-prototype
    env: node
    region: oregon
    plan: free
    rootDir: ./ttcecs-prototype
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
